{
  "name": "dev-task-2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/app.ts",
    "build": "tsc",
    "start": "node dist/app.js",
    "worker:ownership": "ts-node-dev --respawn --transpile-only src/workers/ownership.worker.ts",
    "worker:dlq": "ts-node-dev --respawn --transpile-only src/workers/dlq.worker.ts",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint . --ext .ts",
    "clean": "rm -rf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/supertest": "^7.2.0",
    "jest": "^30.3.0",
    "supertest": "^7.2.2",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@casl/ability": "^6.8.1",
    "@casl/mongoose": "^8.0.5",
    "express": "^5.2.1",
    "mongoose": "^9.5.0"
  }
}
