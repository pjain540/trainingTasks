// eventloop allows java script to be non blocking and asynchrounous, as javasctipt is an single threaded it executes everything in call stack so if synchrounous request comes it executes immediately and if asynchrounous comes then it is added to OS, thread pool event loop keep watches call stack and call back queue if any task is in callback queue and call stack is empty then eventloop put the task in callstack and callstack executes .